name: Cirrata Custom Build

on:
  workflow_dispatch:

jobs:
  build-cirrata-clients:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "cirrata"
